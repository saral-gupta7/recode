# Direct, non-pooled Neon URL used by migrations, API, and worker.
# Copy this file to .env, replace every placeholder, and never commit .env.
# Copy the direct connection string from Neon's Connect dialog.
RECODE_COMPOSE_DATABASE_URL=postgresql://USER:PASSWORD@NEON_HOST/neondb?sslmode=require&channel_binding=require
