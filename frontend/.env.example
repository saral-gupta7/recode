# Server-only origin used by Next.js rewrites. Do not expose this as NEXT_PUBLIC_*.
RECODE_API_ORIGIN=http://localhost:8080

# Canonical public origin used for metadata, Open Graph, robots, and sitemaps.
RECODE_SITE_URL=https://example.com
